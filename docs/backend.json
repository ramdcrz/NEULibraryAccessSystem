
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user profile within the NEU Library Access System, storing their basic information and access role.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity. Corresponds to Firebase Authentication UID."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "role": {
          "type": "string",
          "description": "The access role of the user within the system. (enum: 'admin', 'user')"
        },
        "user_type": {
          "type": "string",
          "description": "The classification of the user (e.g., 'Student', 'Staff', 'Employee').",
          "enum": ["Student", "Staff", "Employee"]
        },
        "college_office": {
          "type": "string",
          "description": "The college or office the user is affiliated with. Can be null."
        },
        "isBlocked": {
          "type": "boolean",
          "description": "Indicates if the user's access to the system is blocked. Defaults to false."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user account was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "role",
        "isBlocked",
        "createdAt"
      ]
    },
    "VisitLog": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "VisitLog",
      "type": "object",
      "description": "Records each visit to the library by a user, including the purpose and time.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the VisitLog entity."
        },
        "uid": {
          "type": "string",
          "description": "Reference to the User's unique ID who made the visit."
        },
        "email": {
          "type": "string",
          "description": "The email of the user at the time of the visit.",
          "format": "email"
        },
        "userType": {
          "type": "string",
          "description": "The classification of the user at the time of visit (e.g., 'Student', 'Staff', 'Employee')."
        },
        "college_office": {
          "type": "string",
          "description": "The college or office affiliation at the time of visit (Snapshotted)."
        },
        "reason": {
          "type": "string",
          "description": "The purpose or reason for the user's visit to the library."
        },
        "timestamp": {
          "type": "string",
          "description": "Server timestamp when the visit was recorded.",
          "format": "date-time"
        },
        "entryDate": {
          "type": "string",
          "description": "The date of the visit in YYYY-MM-DD format.",
          "format": "date"
        }
      },
      "required": [
        "id",
        "uid",
        "email",
        "userType",
        "college_office",
        "reason",
        "timestamp",
        "entryDate"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/backend/entities/User"
      },
      "description": "Stores user profile information."
    },
    "/visit_logs/{visitLogId}": {
      "schema": {
        "$ref": "#/backend/entities/VisitLog"
      },
      "description": "Stores individual visit logs in a flat, top-level collection."
    }
  }
}
